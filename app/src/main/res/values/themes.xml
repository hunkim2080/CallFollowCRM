<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.CallFollowCRM" parent="android:Theme.DeviceDefault.Light.NoActionBar">
        <item name="android:windowBackground">@android:color/white</item>
        <item name="android:statusBarColor" tools:targetApi="l">@android:color/white</item>
        <item name="android:windowLightStatusBar" tools:targetApi="m">true</item>
        <!-- Android 16(API 36) edge-to-edge 강제 임시 opt-out — 기존 레이아웃(시스템바 배경 유지) 보존. (2026-07-22 targetSdk 36 상향)
             ⚠️ 임시 조치: 추후 구글이 이 opt-out 을 제거 예정 → 정식 edge-to-edge(인셋 직접 처리)로 전환해야 함. -->
        <item name="android:windowOptOutEdgeToEdgeEnforcement" tools:targetApi="35">true</item>
    </style>
</resources>
