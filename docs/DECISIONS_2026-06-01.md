# 2026-06-01 · Claude 추천으로 진행한 결정들 (사장님 아침 확인용)

사장님이 "내 결정이 필요한 부분은 너 추천대로 진행하고, 뭘 추천으로 했는지 남겨놔" 라고 하셔서,
오늘 밤 작업하면서 **사장님 대신 제가 판단한 것들**을 여기 모읍니다. 마음에 안 드는 항목은
말씀해주시면 바로 바꿉니다. (전부 정산 Phase 1 관련 + 현황판)

---

## 1. 현황판 = HTML 대시보드 + 공유 마크다운 둘 다
- 사장님이 "HTML 대시보드"를 고르셨고, 저는 거기에 더해 **서버 Claude도 보게 마크다운(SoT)** 도 만들었어요.
  - 보기용: `design-preview/status-board.html` (브라우저, 클릭해서 펼침)
  - 기준표: `docs/IMPLEMENTATION_STATUS.md` (양쪽 Claude 공유)
- **이유:** 서버(맥미니) Claude는 HTML을 매번 열기 번거로우니, git에 올라가는 마크다운이 동기화에 유리.

## 2. 첫 구현 = 정산 "Phase 1" 만 (3단계로 쪼갬)
- 정산 프로토 기능이 19개라 한 번에 다 하면 위험 → **DB 안 건드리는 미수금 화면부터**.
- **Phase 1(오늘 완료):** 미수금 화면 + 계약금/잔금 토글 + 완납확인/취소.
- **Phase 2(보류):** 현금흐름 4색 달력 + 직접 현금 기록 → **새 저장공간(DB v20) 필요**.
- **Phase 3(보류):** 일당 자동 차감 + 순이익 → 수첩(일당) 만든 뒤 가능.
- **이유:** Phase 2는 DB 마이그레이션이라 잘못되면 **앱이 폰에서 안 켜질 위험**이 있어요.
  과거에도 마이그레이션 불일치로 크래시 난 적이 있어서, **사장님 폰 검증 없이 밤에 강행하지 않았습니다.**
  → Phase 1을 폰에서 확인하신 뒤 Phase 2 진행을 추천.

## 3. 정산 진입점 = "홈 미수금 카드" (사장님이 고르심)
- 홈 KPI 카드 줄 아래에 **"아직 못 받은 돈 OO원 · N곳"** 카드 추가 → 누르면 정산 화면.
- 하단탭 4개(상담함/일정/정산/더보기) 전면 도입은 **앱 전체 네비 구조를 바꾸는 큰 작업**이라
  Phase 1 범위에서 뺐어요. (나중에 별도 결정)

## 4. 미수(못 받은 돈) 계산 규칙 — 제가 정한 로직
- **받을 돈(total)** = 총액. 총액 없으면 (계약금 + 잔금).
- **잔금 금액** 안 적었으면 = (총액 − 계약금) 으로 자동 추정. (사장님이 직접 적었으면 그게 우선)
- **받은 돈** = 받음 체크된 계약금/잔금 합.
- **완납** = 받은 돈이 받을 돈 이상.
- 돈 정보가 하나도 없는 고객은 정산 목록에서 **제외**.
- **이유:** CustomerEntity의 기존 주석("balance = total − deposit")과 일치시킴. 7가지 경우를 테스트로 박아둠.

## 5. 완납 오탭 방지 방식
- 잔금 "받음" 켤 때 → **"전액 받았어요?" 확인 한 번** 띄움. (실수로 완납되는 것 방지)
- 완납된 카드엔 **"완납 취소"** 글자 → 되돌리기.
- **이유:** 예전에 사장님이 "버튼 잘못 눌러 전액완납됐다"고 하신 통점 반영.

## 6. 토글 시 자동 카테고리 갱신 유지
- 정산에서 계약금/잔금 받음 바꾸면, 고객상세와 똑같이 **자동 분류(reclassify)** 도 호출.
- **이유:** 같은 데이터를 두 화면(정산/고객상세)에서 만지니 동작을 일치시켜 혼란 방지.

## 7. 작은 것들
- 금액 표시 = `1,200,000원` 형식(천 단위 콤마) — `MoneyFormatter` 새로 만듦.
- 미수 0원이면 빨강 대신 **"못 받은 돈 없어요 👍"** 긍정 문구(매출 불안 완화 톤).
- 빌드용 JDK = Android Studio 번들 JBR 경로 사용(`JAVA_HOME` 자동 지정).

---

## 검증 상태
- ✅ `assembleDebug` (APK 빌드) 성공
- ✅ `SettlementCalcTest` 7케이스 통과
- ⏳ **사장님 갤S9 실제 동작 확인은 아침에** (홈 미수금 카드 → 정산 → 토글)

---

# 정산 Phase 2 (현금흐름) — 사장님 "2 가자" 지시로 진행 + 제가 정한 세부

사장님이 Phase 2 진행을 지시 → 아래는 제가 판단한 부분:

## 8. DB는 "새 테이블 추가"로만 (가장 안전한 마이그레이션)
- `manual_cash` 테이블 신규 추가(v19→v20). **기존 데이터/테이블은 안 건드림** = 깨질 위험 최소.
- 안전망: 혹시 마이그레이션 실패해도 `fallbackToDestructiveMigration`이 켜져 있어 앱이 안 죽음(최악의 경우 캐시성 데이터만 리셋).
- ⚠️ 그래도 **폰 첫 실행 때 실제로 마이그레이션이 돌아요** → 아침에 정산 들어가보시고 이상 없는지 확인 필요.

## 9. 현금흐름 = 정산 화면 안 "탭"으로 (별도 화면 X)
- 정산 화면 위에 **[미수금] [현금흐름]** 탭 두 개. 하나의 정산 안에서 전환.
- 이유: 프로토도 정산 안에 같이 있었고, 화면을 또 만들면 길 찾기 복잡.

## 10. 현금흐름에 자동으로 들어오는 돈 (제가 정한 규칙)
- **들어온 돈(확정)** = 계약금/잔금 받은 날에 자동 표시.
- **들어올 돈(예정)** = 아직 못 받은 미수 + 시공 예약일 있으면 그 날에 표시.
- **직접 기록** = 사장님이 손으로 적는 현금(자재비 등). 수입/지출·완료/예정 선택.
- 4색: 들어온(진파랑)/들어올(연파랑)/나간(빨강)/나갈(연빨강) — 프로토 색 그대로.

## 11. 순이익 두 가지로 표시
- **확정 순이익(실현)** = 실제로 오간 돈만. **예상 순이익(추정)** = 예정까지 포함.
- 월매출 전월대비는 Phase 3로 미룸(일당 자동차감과 함께).

## 남은 추천 (사장님 결정 대기)
- 일당 자동차감/월매출 = **수첩(일당) 만든 뒤** Phase 3 추천.
- 하단탭 4개 전면 도입 여부 (지금은 홈 진입 + 정산 내 탭).
- 정산 화면에서 금액(총액/계약금) **직접 입력**도 넣을지 (지금은 고객상세에서만).

---

# "100%까지 다이렉트로" 연속 진행 — 추가 추천 결정 (2026-06-01 오후)

사장님 "100%까지 다이렉트, 컨펌 몰아서" 지시로 컨펌 없이 계속 구현. 이번 배치에서 제가 정한 것:

## 12. 홈 오늘 시공 히어로 = 다크 카드, 진입점은 자동
- 시공 당일이면 상담함 맨 위 다크 히어로(고객·주소·길찾기), 없으면 다음 시공 미리보기/긍정 카드.
- 별도 설정 없이 자동. 주소 없으면 "주소 미등록 — 탭해서 등록".

## 13. 셀프 일정 등록 진입 = 일정 화면 FAB
- 일정(시공 예약) 화면 우하단 "일정 등록" FAB → 폼. 시공일은 안드로이드 기본 DatePicker 사용(가장 안정적).

## 14. 사업자정보 = 설정에 저장(SharedPreferences)
- 별도 서버/DB 없이 폰에 저장. 견적 유효기간 기본 14일. 견적서/접수서 만들 때 이 값 사용 예정.

## 15. 수첩 = 일당+거래처 한 테이블(kind 구분)로 통합
- 작업량/복잡도 줄이려 테이블 2개 대신 1개(`notebook_contacts`, kind=WORKER/VENDOR). DB v21(새 테이블, 안전).
- 1차 = 기본 CRUD + 분류(자유 입력) + 전화/문자(기본 전화·문자 앱 인텐트). "함께한 현장"·"자주 쓰는 문구"는 다음.

## 진행 현황 (이 시점): 15 → 26 / 51 (51%). 전부 빌드 성공 + main 푸시.
**남은 큰 항목**(헤비/결정필요): 시공접수서(멀티스텝), 견적서 PDF, 정기문자(스케줄러), 비즈니스 리포트(집계),
  팀 관리(**서버 필요**), 온보딩 업종/스토리/막내비서(재설계), 통화 요약카드·다듬기(**서버 Claude 영역**).

---

## 16. 부재중 자동답장 카드 = "최근 24h + AUTO_SENT/실패만" 노출
- 막내(AutoReplyScheduler)가 사장님 대신 자동 발송한 첫 인사 기록을 상담함 상단 카드로 보여줘요.
- **추천 판단 3가지:**
  1) **최근 24시간** 것만 (그 이전은 자동으로 사라짐) — 홈을 깔끔하게 유지. 별도 "정리/밀기" 안 만듦(자동 만료가 더 단순).
  2) **취소(AUTO_CANCELLED) 건은 안 보여줌** — 사장님이 카운트다운에서 본인이 취소한 거라 다시 알릴 필요 없음. 보냄(✅)·실패(⚠)만 표시.
  3) **실패 건은 빨강 강조 + "직접 보내주세요"** — 막내가 못 보낸 건 사장님이 알아야 하니까.
- 탭하면 그 고객 대화창으로. DB 변경 없음(기존 message_histories 테이블 재사용 → 안전).

---

## 17. 시공 시간·기간 = 날짜와 분리한 별도 컬럼(DB v24)
- 일정에 **시작 시간**(오전8~오후4 칩 + 미정)과 **여러 날 기간**(당일~일주일)을 추가.
- **추천 판단:**
  1) **시간은 날짜와 따로 저장** — 기존 `scheduledWorkDate`는 자정(startOfDay)으로 정규화돼 미확인/홈/달력 로직이 다 거기 의존. 시간을 거기 섞으면 그 로직이 깨질 위험 → 분(minute) 컬럼 `scheduledWorkMinutes` 별도. 기존 로직 0 변경.
  2) **여러 날은 "막대"가 아니라 "기간 내 모든 날에 점"** — 프로토는 막대(lane)지만, 막대 렌더는 복잡/버그소지. 같은 가치(어느 날 진행 중인지)를 점으로 표현. `jobCoversDay`로 달력·선택일 목록·홈 오늘시공 모두 일관 적용.
  3) **시간/기간 모두 선택**(필수 아님). 미정이면 표시 생략, 당일이면 기간 생략 → 화면 깔끔.
- DB v24 = ALTER TABLE ADD COLUMN 2개(additive, 안전). 일정 영역 2/5→4/5.

---

## 18. 채팅 "내 일정 확인" = 읽기 전용 미니 달력 시트
- 채팅 상단 달력 아이콘 → 바텀시트로 월 미니 달력(시공 있는 날 점) + 날짜 탭 → 그날 시공 목록(이름·시간·주소).
- **추천 판단:**
  1) **읽기 전용** — 프로토는 이 시트에서 바로 일정 잡기도 되지만, 등록은 일정 화면 FAB 로 일원화(중복 진입점 혼란 방지). 여기선 "확인"만.
  2) **모든 날 탭 가능**(프로토는 시공 있는 날만) — 빈 날 탭하면 "비어있어요 — 약속 잡기 좋아요" → 고객과 통화하며 빈 날 찾는 본래 가치 강화.
  3) **달력 로직 중복 최소화** — ScheduleScreen 의 private 헬퍼를 공유 안 하고 시트 파일에 작은 버전(miniJobCoversDay 등) 복제. 두 화면 독립 → 한쪽 수정이 다른쪽 안 깸. (작은 중복 < 결합 위험)
- DB 변경 없음(기존 customers 읽기). **일정 영역 5/5 완료** → 정산·수첩·온보딩·일정 4영역 100%.

---

## 19. 통화 구간 = 렌더 레이어 병합(loadMessages 무손상) + 요약은 서버로 분리
- 채팅 타임라인에 통화 기록을 "📞 수신/발신/부재중 · N분" 청록 카드로 시간순 끼워 보여줌.
- **추천 판단:**
  1) **튜닝된 loadMessages(3-stage 캐시) 절대 안 건드림** — 통화는 별도 flow(callRecords)로 받아 `buildChatTimeline` 이 렌더 직전에만 messages+calls 를 시간순 병합. 메시지 로딩 로직 0 변경 → 회귀 위험 차단.
  2) **suffix 매칭** — 통화 기록 phoneNumber 저장 포맷이 SMS 와 다를 수 있어 끝 8자리로 매칭(SMS 와 동일 방식).
  3) **"요약 가져오기"는 이번 범위서 분리** — 프로토의 에이닷 통화요약→불릿→후속문자 버튼은 서버/에이닷 의존이라 상담함 "통화 요약 카드"(서버 항목)로 넘김. 이번엔 통화 발생 자체를 타임라인에 노출(=구간 표시)까지.
  4) **청록색(#0E9E90) 카드** — 파랑 문자 말풍선과 시각적으로 확실히 구분(프로토 .chat-call 색 그대로).
- DB 스키마 변경 없음(DAO 쿼리 1개 추가). 채팅 4/7→5/7.

---

## 20. 정기문자 = WorkManager 없이 "포그라운드 계산 + 확인 후 발송"
- 시공 후 N일마다 보낼 안부/점검 문자를 규칙으로 등록하고, 보낼 때가 되면 상담함 카드+목록으로 알림.
- **추천 판단(중요):**
  1) **자동 발송 안 함** — 프로토는 "자동 발송"도 있지만, 우리 정책은 SEND_SMS=명시 액션(사장님이 ▶). 또 잘못된 대량 자동발송은 신뢰 치명타. → "보낼 때 됐어요" 목록에서 [보내기] 누르면 **채팅에 본문 채워 진입**, 사장님이 확인하고 ▶.
  2) **WorkManager(백그라운드 스케줄러) 안 씀** — 삼성 배터리 최적화가 주기작업을 자주 죽여 "안 떴다" 통점 위험. 대신 **앱 열 때 포그라운드 계산**(시공인은 앱을 수시로 열어 신뢰성↑, 코드 단순, 배터리 0). 의존성도 안 늘림.
  3) **회차 누적 폭주 방지** — 시공 후 여러 회차가 쌓여도 "오늘 이하 가장 최근 회차 1건"만 노출. 보냄/넘김은 로그로 기록해 중복 제외(`recurring_message_log` unique index).
  4) **순수 계산 분리 + 테스트** — `RecurringDueCalc`(android 비의존) + JUnit 7케이스. 회차/타겟/로그/치환 규칙 회귀 방지.
- DB v25 = 테이블 2개(additive). 정기문자 예약 + 발송 대기 알림 = 2개 항목 동시 완료.
