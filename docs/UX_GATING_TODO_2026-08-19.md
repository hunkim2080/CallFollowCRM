# "조용히 숨김/잠금" UX 안티패턴 — 해야할일 (2026-08-19)

**패턴**: 어떤 상태 때문에 버튼/섹션/기능이 비활성·회색·숨김 되는데, 사용자에게 **(1) 왜 안 되는지 (2) 어떻게 하면 되는지**를 화면에서 안 알려주는 곳 → "왜 안 되지?" 헤맴.
계기: 사장님(+테스터) 블로그 스튜디오에서 진행중 현장이면 [글 만들기]가 조용히 잠겨 헷갈림 → "uiux부재" 지적.

> ⚠️ 착수 전 각 항목 **실제 코드 다시 확인**(줄번호는 2026-08-19 감사 시점, 이동 가능). 앱 파일 경로 = `app/src/main/java/com/detailline/callfollowcrm/`.

---

## ✅ 완료 (2026-08-19)
- **[웹] 글쓰기 잠금 상태 카드** — 진행중 현장이면 "🔒 완료하면 열려요 + 여는 법(앱에서 잔금/완료) + 왜" 크게. `renderRight` early-return. 배포 `ed035a72`. 프로토 88679745.
- **[웹] Gemini 키 미등록 카드** — 키 없으면 "🔑 AI 키 먼저 등록 → [마이페이지]" 카드(빨간글씨 실패 대신). `web_viewer` has_key 주입 + `renderRight`. 배포 `02922fd`. **테스터 전원이 첫 글 때 부딪히던 것.**

---

## 🔴 상 — 우선

### 1. 채팅 "고객 아님" 오탭 → 도구 4개 통째 증발 + 되돌리기 숨음
`chat/ChatScreen.kt:594, 837-839, 920-921` (분류 행 810-826)
- `isPlainThread=true` 되면 ⓘ고객카드(594)·✨AI다듬기·⊕액션(920-921)·AI추천 띠(837-839)가 **동시에 사라짐**. 되돌릴 '고객카드'까지 같이 숨어 **오탭하면 영구처럼 보임**.
- **Fix**: 문자함(고객아님) 상단에 상시 한 줄 — "고객이었나요? [고객으로 되돌리기]".

### 2. 광고 오판 문자 상담함서 흔적 없이 증발 + 복구 UI가 죽은 코드
`home/HomeScreen.kt:983-994`(필터) · `3287`(AdBoxHeader) · `3301`(AdRow) · `234`(adBoxExpanded)
- `isLikelyAd` 참이면 상담함서 제거. 진짜 문의 오판 시 증발. **복구 UI(`AdBoxHeader`/`AdRow`/`adBoxExpanded`)는 정의만 되고 호출되는 곳이 없음 = 죽은 코드.**
- **Fix**: 죽은 복구 UI를 상담함 하단에 실제 연결 → "📁 광고 N건 자동으로 치웠어요 / 이건 광고 아님" 복구. (만들어져 있어서 값쌈)

---

## 🟠 중

### 3. "내 업종" 직접추가 입력칸이 살아있는 척하는데 완전 죽음 (웹 사례 판박이)
`trade/TradeSelectScreen.kt:100, 121-129`
- 줄눈·에어컨 외 칩 회색+안눌림(`100`). 그 아래 `placeholder="예: 방역, 인테리어 필름…"` 입력칸+"추가" 버튼 `enabled=false`(`126`). 왜 회색인지·언제 열리는지 화면 안내 0(사유는 "테스터 빌드" 주석뿐).
- **Fix**: 회색 칩/입력칸 아래 한 줄 — "지금은 줄눈·에어컨만 지원해요. 다른 업종은 순차 오픈 예정".

### 4. AI 추천이 홈·채팅 양쪽서 사라지는데 "설정에서 켜기" 실마리 없음
`chat/ChatScreen.kt:835-839` + `home/HomeScreen.kt:3503-3515` (토글 `settings/SettingsScreen.kt:2298`)
- `aiReplyPrepEnabled=false`면 조용히 사라짐. 설정 토글 문구는 양호하나(2291-2295), 끈 걸 잊으면 되돌아갈 실마리가 채팅·홈에 없음.
- **Fix**: OFF일 때 담백한 자리 옆 작은 "AI 추천 꺼짐 · 켜기"(설정 딥링크).

### 5. "✨ 요약 작성 중" 스피너가 서버 다운 시 무한 회전 (끝 상태 없음)
`home/HomeScreen.kt:2714-2720`
- `aiCardSummary==null` 무한. 끝난 시도에만 `""` 센티넬로 사라짐. 실패 신호 = 작은 빨간 점뿐.
- **Fix**: N초 후 "요약을 못 불러왔어요 · 잠시 후 자동 재시도"로 전환.

### 6. 접수서/통화내용/팀사진 알림 카드 — 데이터 생기기 전엔 진입점 자체가 안 보임
`home/HomeScreen.kt:747` (주석 "데이터 생기면 노출(지금 숨김)"). 요금제상 되는 기능인데 존재를 모름(플레이스홀더 없음). 심각도 낮은 편.

---

## 🟡 하 — 한 줄 안내로 값싸게

- **7.** 협업 사장님 부르기 보내기 — `bizPhone` 미등록이면 탭 후 토스트만. `customer/CustomerDetailScreen.kt:3458-3464` (채팅 견적 발행 `ChatScreen.kt:4907·4912`도 같은 결). "어떻게"는 알려주나 사전 힌트 없음.
- **8.** 기본 문자앱이면 '고객 사진(문자) 받기' 행 조용히 사라짐. `settings/SettingsScreen.kt:420-424`. 방어 목적이라 합리적이나 찾던 사람에겐 사유 없음.
- **9.** 설정 '비즈니스'·'프로' 배지가 잠금(유료)처럼 보이는데 실제 다 열림. `settings/SettingsScreen.kt:388·414` vs 해명 `449-453`("베타 무료")가 화면 저 아래로 멀어 오해. → 배지 옆 즉석 "베타 무료" 표기.
- **10.** 오늘 시공 카드 '꾹 눌러 순서 바꾸기' 발견 불가(2건+ 일 때만). `home/HomeScreen.kt:1790-1796`. (잠금 아닌 발견성)
- **11.** 협업 현장 줄 `shareId` 비면 화살표 보이는데 탭 무반응. `home/HomeScreen.kt:1975`. (엣지, 낮음)

### 웹 (마이너)
- **재료 빈칸** — 통화요약/내메모 "아직 없어요"만, 어떻게 채우는지 힌트 없음(완료 고객 문자·통화가 모여요 / 앱 메모하면 나와요).
- **사진 없음** — "사진이 아직 없어요"만, 앱에서 올리면 나온다는 안내 없음.

---

## 참고: 이미 잘 된 곳 (기준·건드리지 말 것)
통화녹음 미연결 안내 `ChatScreen.kt:2072` "[연결 설정하러 가기]" · 오버레이 권한 `SettingsScreen.kt:2128` · 웹로그인 게이트 `1217` · 견적 빈 가격표 `PricingItemsScreen.kt:213` "+ 버튼으로 추가 / ✨문자에서 자동채우기" · 저장 메시지 빈상태 `ChatScreen.kt:983` · 협업 탭 `CustomerDetailScreen.kt:515` "먼저 '일정·정산'에서 시공일을" · 온보딩 연결 마법사 전체 · 말투학습 플레이스홀더 `StyleLearningScreen.kt:175` · 팀원 미리보기 `TeamScreen.kt:208` — 전부 "왜+어떻게" 화면에 씀.

**추천 착수 순서**: 1 → 2 → 3 (오탭 영구화·문의 증발·죽은 입력칸 = 혼란 최대). 나머지는 한 줄 안내/링크로.
