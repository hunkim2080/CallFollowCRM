# 서버(맥미니) Claude 핸드오프 — 2026-06-02 (안드로이드 22개 기능 완료 후)

> 이 문서는 `SERVER_HANDOFF_2026-06-01.md` 를 **대체**합니다(최신 권위본).
> 작업 시작 전 `git pull --rebase` 후 이 문서 + `docs/IMPLEMENTATION_STATUS.md` 확인 바랍니다.
> CLAUDE.md 룰: 안드로이드 Claude 는 `server/` 를 안 만집니다 → 아래는 **요청/공유**.

---

## 0. 현재 상태 한 줄
- 안드로이드 단독 구현 **전부 완료** (현황판 45/52, 약 87%). 이번 세션 22개 기능.
- **남은 7개는 전부 서버 영역 또는 서버 의존** → 이제 공은 맥미니 Claude 쪽입니다.
- 안드로이드는 DB v25 까지(additive). 모든 신규 기능은 로컬 DB + 포그라운드 계산이라 **서버 변경 없이** 동작.
  서버가 해줘야 앱이 "조용히 숨긴 카드"가 켜지는 항목들이 아래입니다.

## 1. 공유 기준표 (같이 관리)
- **`docs/IMPLEMENTATION_STATUS.md`** = 프로토 ↔ 실제 앱 SoT. 보기용 = `design-preview/status-board.html`.
- 서버 항목 상태가 바뀌면 이 표의 해당 항목(아래 매핑)을 같이 갱신해 주세요.

---

## 2. 남은 7개 — 서버 작업 명세 (우선순위 순)

### (P1) AI 다듬기 ✨ — 품질 업 (Claude 전환) · 채팅
- **현황:** 앱은 이미 `POST /api/refine` 를 호출 중 (`RemoteRefineRepository`). 동작은 하나 **품질 부족**(과거 Ollama).
- **앱 측 계약(그대로 유지하면 됨):**
  - 요청 바디: `input`(사장님 초안) + `RefineContext { recentMessages[], ownerToneSamples[], customerName, customerMemo }`.
  - 응답: 다듬은 문장 1개(평문). 실패 시 앱이 "Tailscale 확인" 토스트.
- **요청:** `/api/refine` 내부를 **Claude (또는 좋은 모델)** 로 + 사장님 톤 샘플 활용해 자연스럽게. 시그니처 변경 불필요.
- 현황판: 채팅 "다듬기 ✨" 🔶→✅ 으로.

### (P1) 통화 요약 카드 · 상담함 + 채팅
- **현황:** 앱은 통화 구간(📞 카드)을 채팅 타임라인에 이미 표시(`CallSegment`). 단 **요약 본문은 비어 있음**.
  - observe 지점: `AiSummaryEntity.conversationSummaryJson` (ChatScreen 상단 요약 박스) + `CallSummaryEntity`(callSummaryRepository).
  - 앱은 응답 없으면 박스를 **조용히 숨김**.
- **요청:** 에이닷 통화요약 텍스트(사장님이 공유) → 서버 LLM 정리 → 위 캐시에 채워주기.
  - 입력 후보: 통화 transcript/에이닷 요약 + recentMessages + customer hint.
  - 출력: 불릿 몇 줄 + (선택) "후속 문자 초안".
- 현황판: 상담함 "통화 요약 카드" 🔷→✅, 채팅 "통화 구간 표시"의 요약 부분.

### (P2) 시공접수서 (고객 자가확인 폼) · 견적·접수서
- **현황:** 앱 미구현. 프로토에 폼 흐름 있음.
- **요청(서버 주도가 자연스러움):** 고객이 링크로 주소·시공범위·연락처를 직접 확인/입력하는 웹 폼 + 제출 저장.
  - 앱 연동 지점(추후): 사장님이 채팅에서 "접수서 링크 보내기" → 서버가 발급한 URL 을 SMS 본문에 삽입.
  - 제출되면 서버 → 앱 알림(푸시 또는 polling)으로 "접수서 작성됨".
- 의존 항목: **"접수서 작성 리마인드"**(상담함) 는 이게 생겨야 가능. 앱 측 리마인드 인프라(아래 4번)는 이미 있음 → 접수서 발급/제출 시각만 주면 앱이 카드로 띄울 수 있음.

### (P2) 팀 관리 (99k 요금제) · 더보기
- **현황:** 앱 미구현. 다계정/권한 = 서버 필수.
- **요청:** 팀원 초대·역할·현장 배정 모델 + API. 99k 티어 게이팅.
- 의존 항목: **"팀원 현장사진 알림"**(상담함) 은 팀 모델 + 사진 업로드 채널이 생겨야 가능.

### (P3) 접수서 작성 리마인드 · 상담함 (← 시공접수서 후)
- 앱은 **리마인드 인프라 재사용 가능**: 정기문자/시공안내/견적회신과 동일 패턴
  (`recurring_message_log` + 홈 카드 + 보내기/넘기기). 서버가 "접수서 미작성 + 발급 N일 경과" 신호만 주면 앱이 카드화.

### (P3) 팀원 현장사진 알림 · 상담함 (← 팀 관리 후)
- 팀원이 올린 현장 사진을 사장님 상담함에 알림. 팀/스토리지 채널 생긴 뒤.

---

## 3. 서버가 이미 책임지는 것 (점검만)
앱이 응답 없으면 조용히 숨기므로, 실제로 도는지 확인 부탁:
1. **prepare-reply** (`POST /prepare-reply`, `ServerSuggestionRepository`) — AI 추천 답변 칩. 정상 동작 중으로 보임.
2. **cardSummary** (`AiSummaryEntity.cardSummary`) — 홈/일정/고객상세 카드 ✨ 한 줄 요약. 생성·캐시 확인.
3. **conversation summary** — 위 통화 요약 카드와 같은 캐시.

## 4. 앱이 이미 갖춘 "리마인드/발송 대기" 인프라 (서버가 재활용하면 좋음)
이번 세션에 만든 **공통 패턴** — 서버 신호를 홈 카드로 띄우기 쉬움:
- DB v25 `recurring_message_log` (ruleId, customerId, occurrenceDayStartMs, status) — 보냄/넘김 dedupe.
  - 양수 ruleId = 정기문자, **음수 sentinel** = 시스템 리마인드(-1 D-1, -2 도착, -4 견적회신). 새 리마인드는 -5, -6… 추가 가능.
- 홈 `SimpleDueCard` + 전용 목록 화면 + `[보내기]=ChatDraftStore prefill 후 채팅 진입` / `[넘기기]=DISMISSED 로그`.
- **정책 고정:** 자동발송 X. 모든 발송은 사장님이 채팅에서 ▶ (SEND_SMS 명시 액션). 서버도 이 정책 존중.

## 5. 서버 변경 불필요 (이번 세션 안드로이드 단독, 참고)
- 정산(미수금·현금흐름·순이익·월매출·일당 자동차감) = 로컬 `CustomerEntity`/`ManualCash`/`JobCrew`.
- 수첩·셀프일정·시공시간/기간·내일정·정기문자·시공 D-1/도착·견적 회신 리마인드 = 전부 로컬 DB + 포그라운드 계산.
- 향후 **비즈니스 리포트의 "전국 평균 대비"** 시장 인사이트로 가면 그때 서버 집계 API 필요 → 별도 요청 예정.

---

## 6. 로드맵상 미리 생각해둘 것 (지금 X)
- **시장 인사이트 카드**(`project_business_goal_pricing`): 10만명 익명 집계 = 시장 인덱스. 데이터 모델만 염두에.
- **블로그 후기 글 생성**(유료 훅): 대화 + 현장사진 → 포스팅 초안. 서버 LLM 후보.

---

## 요약 한 줄
> 안드로이드 단독 항목 **100% 소진**. 남은 7개는 전부 서버/서버 의존.
> **1순위 = /api/refine 품질(Claude) + 통화 요약 캐시 채우기.** 그 다음 시공접수서/팀 관리(둘이 의존 항목 2개를 풂).
> 앱엔 리마인드/발송-대기 공통 인프라가 이미 있으니, 서버는 **신호(시각/상태)만** 주면 앱이 홈 카드로 띄웁니다.
