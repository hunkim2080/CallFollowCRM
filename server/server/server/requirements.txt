fastapi
uvicorn
httpx
pydantic
anthropic
python-multipart
# §16 — Tone RAG (4단계 킬러콘텐츠)
# bge-m3 (BAAI) 임베딩 모델 — 한국어 강함 + multilingual + 1024 dim.
# FlagEmbedding 이 안 깔리면 sentence-transformers 로 대체 가능.
FlagEmbedding
# sqlite-vec — sqlite KNN 벡터 검색. vec0 virtual table 제공.
sqlite-vec
# §26 — 통화 녹음 STT (안드로이드 SERVER_HANDOFF 2026-06-08)
# faster-whisper = CTranslate2 기반 Whisper, CPU 모드 OK, 한국어 지원.
# base 모델 ~75MB, 첫 호출 시 자동 다운로드.
faster-whisper
# §30 — FCM 푸시 (안드로이드 SERVER_HANDOFF_fcm_push 2026-06-12)
# 협업 invite/progress/paid 시점에 앱 꺼져 있어도 즉시 알림.
# Firebase service account JSON 경로 = ENV FCM_SERVICE_ACCOUNT_JSON.
firebase-admin
pillow>=10.0
# 웹 마이페이지 — owner 본인 Gemini API 키 암호화 저장(Fernet). (2026-08-15)
cryptography
